import React from "react";
import { Text, Surface } from "react-native-paper";

const Parts = () => (
  <Surface style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
    <Text variant="titleLarge">Parts</Text>
  </Surface>
);

export default Parts;
